/*
//boolean 
const eAzul = true;
const aAzul = false;

// >, >=, <, <= 
*/
// resto da divisão 
const numero = 2;

//const numeroPar = numero %2
const numeroDivisil = (numero % 2) === 0; // 2(==) ou 3(===) igual(=) comparação

if (numero === 0){
    console.log('Numero Invalido')
} else if (numero){
    console.log('Sim ele é Par')
} else {
    console.log('Não, Ele é Impar')
}